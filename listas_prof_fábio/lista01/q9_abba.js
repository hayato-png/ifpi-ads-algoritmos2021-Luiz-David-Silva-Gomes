// leia 2 numeros (a,b) e escreva-os em ordem inversa (b,a)
const prompt = require('prompt-sync')()

// inicio 
console.log('digite um número e o veja em ordem inversa')
const a = Number (prompt('digite um numero aqui: '))
const b = Number (prompt('digite outro número aqui: '))

// processo 

// saída 
console.log(`em ordem inversa é:(${b} , ${a})  `)